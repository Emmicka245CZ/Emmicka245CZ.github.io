<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ page.title | default: site.title }}</title>
  {{ content_for_head }}
  <style>
    .home-button {
      position: fixed;
      bottom: 20px;
      right: 20px;
      padding: 10px 14px;
      border-radius: 50%;
      background: #24292f;
      color: white;
      text-decoration: none;
      font-size: 16px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      z-index: 1000;
    }

    .home-button:hover {
      background: #57606a;
    }
  </style>
</head>
<body>

  {{ content }}

  <a href="{{ site.baseurl }}/" class="home-button" title="Home">🏠</a>

</body>
</html>
